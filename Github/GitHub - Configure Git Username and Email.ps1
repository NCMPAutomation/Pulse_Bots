# Define the username and email
$username = "your-username"
$email = "your-email@example.com"

# Configure the username and email
git config --global user.name $username
git config --global user.email $email
